import React from "react";
import {
    SafeAreaView,
    ScrollView,
    StyleSheet,
    Text,
    View,
    Image,
  } from 'react-native';

const MyMusic = () => {
    <SafeAreaView>
        <ScrollView contentInsetAdjustmentBehavior="automatic">
            <View>
            <Text>My Music</Text>
            </View>
        </ScrollView>
    </SafeAreaView>
}

export default MyMusic